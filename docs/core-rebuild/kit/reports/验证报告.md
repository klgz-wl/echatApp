# Git规则版材料包验证记录

2026-09-21，工具及参考提交 273835a。源码仓库和实际导出包各运行 Python 测试58项，全部通过。

new支持仅有有效.git的目录；测试覆盖Git元数据不变、原文件保护、发布失败回滚、已有AGENTS规则保留及重复运行不重复追加。new/existing均生成每任务中文commit/push规则。

social03_android已使用new模式装配，初始化前后.git所有文件SHA256逐项相同。当前仅基线接入，实际Core重构及Model扩展未实施。

目标已在JDK17完成212项JVM测试、devDebug/devRelease/prodDebug构建和Lint（0错误/46警告）。首次验证因Finder新增.DS_Store造成输入快照变化而正确拒绝通过；本版仅排除此类系统元数据并补充测试，实际源码变化仍拒绝。此包封装时尚未完成修复后的完整verify/APK核验，不将已构建冒称完整验证通过。Social03目标工程将独立执行doctor、verify并保存初始化验证报告；其结果不等于完整重构验收。

普通包不含签名私钥和密码，sharedDev签名从独立私密附件装配。未安装应用，未执行真实购买、付费生成或平台收数验收。
